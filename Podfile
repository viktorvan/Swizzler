# Uncomment this line to define a global platform for your project
# platform :ios, '6.0'

target 'Swizzler' do

end

target 'SwizzlerTests' do
  pod 'Expecta', '~> 0.3'   # expecta matchers
  pod 'OCMockito', '~> 1.0'
end

